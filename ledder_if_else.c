#include <stdio.h>

main()
{

    int a;

    printf("enter the  value :");
    scanf("%d", &a);

    if (a > 0)
    {

        printf("the value is positive ");
    }
    else if (a > 0)
    {
        printf("the value is nagetive");
    }
    else
    {

        printf("value is neutral");
    }
}